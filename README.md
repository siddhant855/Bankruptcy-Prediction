# Bankruptcy-Prediction
A fully interpretable bankruptcy prediction system built using manual Logistic Regression, mini-batch gradient descent, and threshold tuning. Designed as a transparent financial early-warning model for detecting corporate distress using real accounting ratios.
